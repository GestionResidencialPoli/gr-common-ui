# Demo frontend only. No backend URL or real credentials are needed.
# Development defaults to demo if this variable is absent.
# Production defaults to an unconfigured service; set demo only for a showcase.
NEXT_PUBLIC_AUTH_MODE=demo
